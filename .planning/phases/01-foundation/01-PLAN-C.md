---
phase: 01-foundation
plan: C
type: execute
wave: 3
depends_on:
  - 01-PLAN-A
  - 01-PLAN-B
files_modified:
  - .planning/DEPLOY.md
autonomous: true
requirements:
  - INFRA-03

must_haves:
  truths:
    - "`.planning/DEPLOY.md` exists and documents two deployment strategies: Strategy A (index.php path rewrite, all Hostinger plans) and Strategy B (hPanel document root, Business plan only)"
    - "The deploy guide includes an explicit step to run `npm run build` locally BEFORE uploading any files — and explains why (Node.js not available on Hostinger)"
    - "The deploy guide includes a numbered checklist with a step to verify `yourdomain.com/.env` returns 403 or 404 (not file contents)"
    - "The deploy guide documents all three production .env overrides: APP_ENV=production, APP_DEBUG=false, APP_URL=https://yourdomain.com"
    - "The deploy guide explains that `public/build/` must be uploaded on every deploy (not just the first)"
    - "The deploy guide documents the index.php path rewrite with exact before/after code showing the two require lines to update"
  artifacts:
    - path: ".planning/DEPLOY.md"
      provides: "Step-by-step Hostinger deployment guide covering document root, env vars, .env security, and build workflow"
      contains: "APP_DEBUG=false"
  key_links:
    - from: ".planning/DEPLOY.md"
      to: "public/build/"
      via: "deploy checklist step: npm run build then FTP public/build/"
      pattern: "npm run build"
    - from: ".planning/DEPLOY.md"
      to: ".env security"
      via: "checklist step: verify yourdomain.com/.env returns 403/404"
      pattern: "403"
---

<objective>
Write the Hostinger deployment guide that documents the exact steps to go from a working local Laravel project to a live site on Hostinger shared hosting with correct document root configuration, production .env settings, and verified .env security.

Purpose: Hostinger's default web root is `public_html/` — uploading the full Laravel project there exposes `.env`, `vendor/`, and `config/` files publicly. This guide must be written and verified BEFORE any credentials are placed on the server in Phase 4.

Output: `.planning/DEPLOY.md` — a self-contained reference guide an executor can follow step-by-step to deploy safely.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-A-SUMMARY.md
@.planning/phases/01-foundation/01-B-SUMMARY.md

<interfaces>
<!-- Key deployment facts from research — no further investigation needed. -->

Hostinger context:
  - Default web root: public_html/ (the entire public_html/ directory is served)
  - Node.js: NOT available on shared hosting — `npm run build` MUST run locally
  - PHP: 8.2.x on shared hosting (verify exact version in hPanel before first deploy)
  - Strategy A works on ALL Hostinger plans (no SSH required)
  - Strategy B requires Business plan (SSH + hPanel document root setting)

Project file locations (produced by Plans A and B):
  - Full Laravel project: ~/laravel/ (above public_html/)
  - Vite build output: public/build/ (compiled locally, then FTPd)
  - Laravel public directory: public/ → maps to public_html/ on server

The two path lines to rewrite in index.php for Strategy A:
  BEFORE:
    require __DIR__.'/../vendor/autoload.php';
    $app = require_once __DIR__.'/../bootstrap/app.php';
  AFTER:
    require __DIR__.'/../laravel/vendor/autoload.php';
    $app = require_once __DIR__.'/../laravel/bootstrap/app.php';

Security: APP_KEY is generated by `php artisan key:generate`. Never commit a real .env to git.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the Hostinger deployment guide</name>
  <files>
    .planning/DEPLOY.md
  </files>
  <read_first>
    - C:/Users/Ygor/portifolio/.planning/phases/01-foundation/01-RESEARCH.md (read Pattern 8: Hostinger Deploy and the Anti-Patterns section — all content in DEPLOY.md must be consistent with this research)
    - C:/Users/Ygor/portifolio/.planning/phases/01-foundation/01-A-SUMMARY.md (read to confirm actual package versions installed — reference them accurately in the guide)
    - C:/Users/Ygor/portifolio/.planning/phases/01-foundation/01-B-SUMMARY.md (read to confirm actual file paths used — reference them accurately)
  </read_first>
  <action>
    Create `.planning/DEPLOY.md` with the exact structure below. Every section is required — do not omit any section or step.

    The guide must cover:
    1. Prerequisites (what to have before starting)
    2. Pre-deploy build step (npm run build + what to verify)
    3. Strategy A — index.php path rewrite (all Hostinger plans)
    4. Strategy B — hPanel document root (Business plan only)
    5. Production .env configuration (the three required overrides)
    6. Security verification checklist (six mandatory checks)
    7. Repeated deploy workflow (what to do on every subsequent deploy)
    8. Troubleshooting (the three most common failures)

    Write the file with this exact content:

    ---

    # Hostinger Deployment Guide

    **Project:** Portfólio Ygor Stefankowski
    **Target:** Hostinger Shared Hosting
    **Created:** Phase 1 Foundation
    **Last verified:** (update each time you deploy)

    ---

    ## Prerequisites

    Before starting any deploy:

    - [ ] Node.js and npm available on your LOCAL machine (Hostinger does not have Node.js)
    - [ ] FTP client installed (FileZilla, Cyberduck) OR access to Hostinger hPanel File Manager
    - [ ] Hostinger hPanel credentials
    - [ ] Domain pointed to Hostinger nameservers

    **PHP version check:** Open hPanel → Hosting → Manage → PHP Configuration. Verify PHP 8.2.x is selected. If PHP 8.3 is now available, you may bump `composer.json` from `^8.2` to `^8.3`, but this is optional — `^8.2` works on both.

    ---

    ## Step 1: Build Assets Locally

    Node.js is NOT available on Hostinger shared hosting. You MUST build assets on your local machine before every deploy.

    ```bash
    npm run build
    ```

    Verify the build succeeded:
    - Terminal exits with code 0 (no errors)
    - `public/build/manifest.json` exists
    - `public/build/` contains at least one `.css` file and one `.js` file with version hashes

    **Important:** `public/build/` is not committed to git. You must FTP it to the server on every deploy. If you skip this, the production site will have no styling or JavaScript.

    ---

    ## Deploy Strategy A: index.php Path Rewrite (All Hostinger Plans)

    Use this strategy if you do not have SSH access or Business plan hPanel document root settings.

    ### File Structure on Hostinger

    ```
    ~ (home directory)
    ├── laravel/                   ← Upload FULL Laravel project here
    │   ├── app/
    │   ├── bootstrap/
    │   ├── config/
    │   ├── data/
    │   ├── public/                ← Source of truth for public files
    │   ├── resources/
    │   ├── routes/
    │   ├── vendor/
    │   ├── .env                   ← Production .env goes here (NEVER in public_html/)
    │   └── ...
    └── domains/
        └── yourdomain.com/
            └── public_html/       ← Web root — only public/ contents land here
                ├── build/         ← Copied from laravel/public/build/
                ├── index.php      ← Copied from laravel/public/index.php (then modified)
                ├── .htaccess      ← Copied from laravel/public/.htaccess
                └── ...
    ```

    ### Steps

    1. **Upload the full Laravel project** via FTP to `~/laravel/` (create this directory first)

    2. **Upload `public/` contents** to `public_html/` — copy everything INSIDE `laravel/public/` into `public_html/` (not the `public/` folder itself, its contents)

    3. **Edit `public_html/index.php`** — update the two path constants:

    ```php
    // BEFORE (default Laravel install):
    require __DIR__.'/../vendor/autoload.php';
    $app = require_once __DIR__.'/../bootstrap/app.php';

    // AFTER (path rewrite for Hostinger):
    require __DIR__.'/../laravel/vendor/autoload.php';
    $app = require_once __DIR__.'/../laravel/bootstrap/app.php';
    ```

    4. **Copy `.htaccess`** from `laravel/public/.htaccess` to `public_html/.htaccess`

    5. **Configure production .env** — see Step 3 below

    ---

    ## Deploy Strategy B: hPanel Document Root (Business Plan Only)

    Use this strategy if your Hostinger plan includes SSH access and the hPanel Advanced settings.

    ### Steps

    1. **Upload the full Laravel project** via FTP or SSH to `~/laravel/`

    2. **In hPanel:** Navigate to Hosting → Manage → Advanced → Document Root

    3. **Set Document Root** to: `/home/yourusername/domains/yourdomain.com/laravel/public`
       (replace `yourusername` with your actual Hostinger username)

    4. **Configure production .env** — see Step 3 below

    **Advantage over Strategy A:** No index.php modification needed. The web server points directly at `laravel/public/`, so Laravel's `.htaccess` works exactly as designed.

    ---

    ## Step 3: Production .env Configuration

    On the Hostinger server, in `~/laravel/.env` (NOT in `public_html/`), set these three overrides:

    ```dotenv
    APP_ENV=production
    APP_DEBUG=false
    APP_URL=https://yourdomain.com
    ```

    **Why these matter:**
    - `APP_DEBUG=false` — prevents Laravel's Ignition error page from exposing stack traces, file paths, and environment variables to visitors
    - `APP_URL=https://yourdomain.com` — ensures Vite asset URLs point to your domain, not localhost
    - `APP_ENV=production` — enables production caching and disables development helpers

    **Generate APP_KEY** if deploying for the first time (via Hostinger SSH or hPanel terminal):
    ```bash
    php artisan key:generate
    ```
    Or generate locally and copy the value: `php artisan key:generate --show`

    ---

    ## Step 4: Security Verification Checklist

    After every deploy, verify these six items. If any fail, do NOT proceed with Phase 4 launch.

    - [ ] `https://yourdomain.com/.env` returns **403 or 404** — NOT file contents
    - [ ] `https://yourdomain.com/vendor/` returns **403 or 404**
    - [ ] `https://yourdomain.com/composer.json` returns **403 or 404**
    - [ ] `https://yourdomain.com/nonexistent-route` returns a plain error page (NOT Laravel Ignition with stack trace — confirms APP_DEBUG=false)
    - [ ] Page loads with full CSS styling (no bare HTML — confirms public/build/ was uploaded)
    - [ ] Browser console shows no 404 errors for asset files (confirms manifest.json and build/ contents are complete)

    **If `.env` returns file contents:** Your document root is wrong. The entire Laravel project landed in `public_html/` instead of only `public/` contents. Re-read Strategy A and verify the index.php path rewrite.

    ---

    ## Repeated Deploy Workflow

    Every subsequent deploy (after the first) follows this workflow:

    ```
    1. Make your changes locally
    2. npm run build                             ← Always. Every deploy. No exceptions.
    3. FTP: upload public/build/ to public_html/build/ (replace existing contents)
    4. FTP: upload changed PHP/Blade/config files to ~/laravel/ (preserving directory structure)
    5. If composer.json changed: run composer install --no-dev on server (via SSH or hPanel terminal)
    6. Run: php artisan config:cache (if available via SSH/hPanel terminal)
    7. Verify security checklist items 4, 5, 6 above
    ```

    **What NOT to do:**
    - Never run `npm run dev` and upload the dev server output — dev output is not the same as `npm run build`
    - Never skip uploading `public/build/` — assets are not in git and will be stale if not re-uploaded
    - Never run `npm run build` on the Hostinger server — Node.js is not available

    ---

    ## Troubleshooting

    ### Problem: Page loads with no CSS or JavaScript

    **Cause:** `public/build/` was not uploaded, or build output is from `npm run dev` instead of `npm run build`.
    **Fix:** Run `npm run build` locally, then FTP the entire `public/build/` directory to `public_html/build/`, replacing all existing files.

    ### Problem: `yourdomain.com/.env` returns file contents

    **Cause:** Full Laravel project was uploaded into `public_html/` instead of only `public/` contents.
    **Fix:** Remove all non-public files from `public_html/`. Upload only the contents of `laravel/public/` to `public_html/`. Apply the index.php path rewrite from Strategy A.

    ### Problem: Site shows Ignition error page (Laravel debug screen)

    **Cause:** `APP_DEBUG=true` is set in the production `.env`.
    **Fix:** Edit `~/laravel/.env` on the server. Set `APP_DEBUG=false`. Run `php artisan config:cache` if available.

    ### Problem: All assets 404 with URL `http://localhost/build/...`

    **Cause:** `APP_URL=http://localhost` in production `.env` — Vite generates asset URLs based on this value.
    **Fix:** Edit `~/laravel/.env` on the server. Set `APP_URL=https://yourdomain.com`. Run `php artisan config:cache`.

    ---

    *Guide created: Phase 1 Foundation*
    *Next update: Phase 4 Polish and Deploy — verify all checklist items against live domain*
  </action>
  <verify>
    - `ls .planning/DEPLOY.md` — file must exist
    - `grep "APP_DEBUG=false" .planning/DEPLOY.md` — must return a match (production env var documented)
    - `grep "npm run build" .planning/DEPLOY.md` — must return at least two matches (pre-deploy step + repeated deploy workflow)
    - `grep "403" .planning/DEPLOY.md` — must return a match (security verification step)
    - `grep "Strategy A" .planning/DEPLOY.md` — must return a match
    - `grep "Strategy B" .planning/DEPLOY.md` — must return a match
    - `grep "vendor/autoload.php" .planning/DEPLOY.md` — must return matches showing before/after index.php rewrite
    - `grep "APP_URL=https" .planning/DEPLOY.md` — must return a match
    - `grep "APP_ENV=production" .planning/DEPLOY.md` — must return a match
  </verify>
  <acceptance_criteria>
    - `.planning/DEPLOY.md` exists (grep: `ls .planning/DEPLOY.md`)
    - Guide documents `APP_DEBUG=false` as a required production setting (grep: `grep "APP_DEBUG=false" .planning/DEPLOY.md`)
    - Guide documents `APP_URL=https://yourdomain.com` as a required production setting (grep: `grep "APP_URL=https" .planning/DEPLOY.md`)
    - Guide documents `APP_ENV=production` as a required production setting (grep: `grep "APP_ENV=production" .planning/DEPLOY.md`)
    - Guide includes `.env` security verification step with 403/404 check (grep: `grep "403" .planning/DEPLOY.md`)
    - Guide documents the index.php path rewrite with `../laravel/vendor/autoload.php` (grep: `grep "laravel/vendor/autoload.php" .planning/DEPLOY.md`)
    - Guide documents Strategy A and Strategy B (grep: `grep "Strategy A" .planning/DEPLOY.md` and `grep "Strategy B" .planning/DEPLOY.md`)
    - Guide includes `npm run build` in the repeated deploy workflow (grep: `grep "npm run build" .planning/DEPLOY.md` returns 2+ matches)
    - Guide includes troubleshooting for the three most common failures: no CSS, .env exposed, Ignition debug page (grep: `grep "Troubleshooting" .planning/DEPLOY.md`)
  </acceptance_criteria>
  <done>
    `.planning/DEPLOY.md` exists and documents: prerequisites, pre-deploy build step, Strategy A (index.php rewrite) with exact code, Strategy B (hPanel document root), production .env three overrides, security verification checklist with .env 403 check, repeated deploy workflow, and troubleshooting for four failure scenarios.
  </done>
</task>

</tasks>

<verification>
After task completes:

1. `ls .planning/DEPLOY.md` — file exists
2. `grep "403" .planning/DEPLOY.md` — .env security step documented (INFRA-03)
3. `grep "APP_DEBUG=false" .planning/DEPLOY.md` — production override documented (INFRA-03)
4. `grep "npm run build" .planning/DEPLOY.md` — build-before-deploy requirement documented (INFRA-03)
5. `grep "Strategy A\|Strategy B" .planning/DEPLOY.md` — both deploy strategies documented
6. `grep "laravel/vendor/autoload.php" .planning/DEPLOY.md` — index.php rewrite documented with exact paths
</verification>

<success_criteria>
- `.planning/DEPLOY.md` documents the exact Hostinger document root configuration for both Strategy A and Strategy B (INFRA-03)
- Guide includes the three production .env overrides: APP_ENV=production, APP_DEBUG=false, APP_URL=https://yourdomain.com (INFRA-03)
- Guide includes a security checklist with verification that `yourdomain.com/.env` returns 403/404 (INFRA-03)
- Guide explains why `npm run build` must run locally before every deploy (Node.js not available on Hostinger)
- Guide is actionable: an executor following the steps reaches a deployed site without needing to consult external resources
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-C-SUMMARY.md` with:
- What was written (guide sections, strategies covered)
- Key decisions documented in the guide (which strategy to prefer and why)
- Open items for Phase 4 (verify against live domain)
</output>
