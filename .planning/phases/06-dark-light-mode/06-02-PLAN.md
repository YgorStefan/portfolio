---
phase: 06-dark-light-mode
plan: 02
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - resources/views/partials/nav.blade.php
  - resources/views/pages/home.blade.php
  - resources/views/partials/footer.blade.php
autonomous: true
requirements:
  - THEME-01

must_haves:
  truths:
    - "Botão toggle visível na navbar em desktop (à direita dos links) e em mobile (entre logo e hamburger)"
    - "Clicar no toggle alterna entre sol e lua e troca o tema com transição suave"
    - "Todas as seções de home.blade.php e footer.blade.php refletem corretamente o tema claro"
    - "A preferência do tema é salva no localStorage ao clicar no toggle"
  artifacts:
    - path: "resources/views/partials/nav.blade.php"
      provides: "Botão toggle com SVGs sol/lua, lógica Alpine.js, posição desktop e mobile"
      contains: "localStorage.setItem('theme'"
    - path: "resources/views/pages/home.blade.php"
      provides: "Classes dark: em todos os elementos com cores hardcoded dark"
      contains: "dark:bg-gray-950"
    - path: "resources/views/partials/footer.blade.php"
      provides: "Classes dark: no footer e botão back-to-top"
      contains: "dark:border-gray-800"
  key_links:
    - from: "resources/views/partials/nav.blade.php"
      to: "resources/views/layouts/app.blade.php"
      via: "Alpine.js toggle lê/escreve classe .dark no document.documentElement"
      pattern: "document.documentElement.classList"
    - from: "resources/views/pages/home.blade.php"
      to: "resources/css/app.css"
      via: "Classes dark: ativadas pelo .dark no html que @variant dark do CSS detecta"
      pattern: "dark:bg-"
---

<objective>
Adicionar o botão toggle sol/lua na navbar e aplicar classes dark: em todos os elementos com cores hardcoded no home.blade.php e footer.blade.php.

Purpose: O Plan 01 instalou a infraestrutura CSS/JS. Este plano completa o tema claro tornando todos os elementos visualmente corretos em ambos os modos.
Output: Toggle funcional na navbar; home e footer completamente tematizados com dark: prefixes no Tailwind.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-dark-light-mode/06-CONTEXT.md
@.planning/phases/06-dark-light-mode/06-01-SUMMARY.md

<interfaces>
<!-- Padrões existentes na navbar — replicar exatamente -->

SVG hamburger existente em nav.blade.php (padrão a seguir para sol/lua):
```html
<svg x-show="!open" xmlns="http://www.w3.org/2000/svg" class="h-6 w-6" fill="none" viewBox="0 0 24 24" stroke="currentColor">
    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16" />
</svg>
```
Tamanho: h-6 w-6 | Estilo: fill="none" stroke="currentColor" stroke-width="2"

Estilo do botão hamburger (replicar para toggle):
```html
class="md:hidden text-gray-300 hover:text-white transition-colors p-2"
```

Posição desktop target: dentro de `<ul class="hidden md:flex items-center gap-8">`, como item adicional após os links

Posição mobile target: entre o logo `<a href="#hero">YS</a>` e o botão hamburger `<button @click="open = !open">`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Botão toggle na navbar (desktop + mobile)</name>
  <files>resources/views/partials/nav.blade.php</files>
  <read_first>
    - resources/views/partials/nav.blade.php (ler arquivo completo antes de qualquer edição)
    - .planning/phases/06-dark-light-mode/06-CONTEXT.md (decisões D-07, D-08, D-09, D-10, D-15, D-16)
  </read_first>
  <action>
Editar resources/views/partials/nav.blade.php com as seguintes mudanças:

1. Na tag `<nav>`, adicionar o atributo x-data para o estado do tema (complementando o `open` já existente):
   Substituir:
   ```html
   <nav x-data="{ open: false }" class="fixed top-0 left-0 right-0 z-50 bg-gray-950/90 backdrop-blur-sm border-b border-gray-800">
   ```
   Por:
   ```html
   <nav x-data="{ open: false, dark: document.documentElement.classList.contains('dark') }" class="fixed top-0 left-0 right-0 z-50 bg-gray-950/90 dark:bg-gray-950/90 bg-white/90 backdrop-blur-sm border-b border-gray-800 dark:border-gray-800 border-gray-200 transition-colors duration-150">
   ```

2. Criar a função toggle reutilizável. Adicionar `x-init` na tag nav para definir o método:
   Na tag `<nav>`, adicionar após o x-data:
   ```html
   x-init="$watch('dark', val => {
       if (val) {
           document.documentElement.classList.add('dark');
           localStorage.setItem('theme', 'dark');
       } else {
           document.documentElement.classList.remove('dark');
           localStorage.setItem('theme', 'light');
       }
   })"
   ```

3. Botão toggle DESKTOP: Adicionar como último `<li>` na `<ul class="hidden md:flex items-center gap-8">`, após o `<li>` do "Contato":
   ```html
   {{-- Toggle de tema (desktop) --}}
   <li>
       <button
           @click="dark = !dark"
           class="text-gray-300 dark:text-gray-300 text-gray-600 hover:text-white dark:hover:text-white hover:text-gray-900 transition-colors p-2"
           aria-label="Alternar tema"
       >
           {{-- Ícone sol (visível no dark mode — clique vai para light) --}}
           <svg x-show="dark" xmlns="http://www.w3.org/2000/svg" class="h-6 w-6" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
               <path stroke-linecap="round" stroke-linejoin="round" d="M12 3v1m0 16v1m9-9h-1M4 12H3m15.364-6.364l-.707.707M6.343 17.657l-.707.707m12.728 0l-.707-.707M6.343 6.343l-.707-.707M12 8a4 4 0 100 8 4 4 0 000-8z"/>
           </svg>
           {{-- Ícone lua (visível no light mode — clique vai para dark) --}}
           <svg x-show="!dark" xmlns="http://www.w3.org/2000/svg" class="h-6 w-6" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
               <path stroke-linecap="round" stroke-linejoin="round" d="M20.354 15.354A9 9 0 018.646 3.646 9.003 9.003 0 0012 21a9.003 9.003 0 008.354-5.646z"/>
           </svg>
       </button>
   </li>
   ```

4. Botão toggle MOBILE: Adicionar entre o logo `<a href="#hero">YS</a>` e o botão hamburger `<button @click="open = !open">`. Inserir este bloco entre eles:
   ```html
   {{-- Toggle de tema (mobile) --}}
   <button
       @click="dark = !dark"
       class="text-gray-300 dark:text-gray-300 text-gray-600 hover:text-white dark:hover:text-white hover:text-gray-900 transition-colors p-2"
       aria-label="Alternar tema"
   >
       <svg x-show="dark" xmlns="http://www.w3.org/2000/svg" class="h-6 w-6" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
           <path stroke-linecap="round" stroke-linejoin="round" d="M12 3v1m0 16v1m9-9h-1M4 12H3m15.364-6.364l-.707.707M6.343 17.657l-.707.707m12.728 0l-.707-.707M6.343 6.343l-.707-.707M12 8a4 4 0 100 8 4 4 0 000-8z"/>
       </svg>
       <svg x-show="!dark" xmlns="http://www.w3.org/2000/svg" class="h-6 w-6" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
           <path stroke-linecap="round" stroke-linejoin="round" d="M20.354 15.354A9 9 0 018.646 3.646 9.003 9.003 0 0012 21a9.003 9.003 0 008.354-5.646z"/>
       </svg>
   </button>
   ```

5. Menu mobile dropdown: Adicionar classes de tema no container mobile:
   Substituir:
   ```html
   class="md:hidden border-t border-gray-800 bg-gray-950"
   ```
   Por:
   ```html
   class="md:hidden border-t border-gray-800 dark:border-gray-800 border-gray-200 bg-gray-950 dark:bg-gray-950 bg-white transition-colors duration-150"
   ```
  </action>
  <verify>
    <automated>grep -n "localStorage.setItem('theme'" /c/Users/Ygor/portifolio/resources/views/partials/nav.blade.php && grep -n "dark = !dark" /c/Users/Ygor/portifolio/resources/views/partials/nav.blade.php && grep -n "M20.354 15.354" /c/Users/Ygor/portifolio/resources/views/partials/nav.blade.php</automated>
  </verify>
  <acceptance_criteria>
    - nav.blade.php contém `localStorage.setItem('theme'` (dentro do $watch)
    - nav.blade.php contém `dark = !dark` (nos dois botões toggle)
    - nav.blade.php contém o path SVG da lua `M20.354 15.354A9 9 0 018.646 3.646` (duas ocorrências: desktop e mobile)
    - nav.blade.php contém o path SVG do sol `M12 3v1m0 16v1` (duas ocorrências: desktop e mobile)
    - nav.blade.php contém `x-show="dark"` e `x-show="!dark"` nos SVGs
    - nav.blade.php contém `document.documentElement.classList.add('dark')` no $watch
    - nav.blade.php contém `document.documentElement.classList.remove('dark')` no $watch
    - Botão mobile toggle aparece ENTRE o logo YS e o botão hamburger (verificar ordem no HTML)
  </acceptance_criteria>
  <done>Botão toggle com sol/lua presentes em desktop e mobile; $watch sincroniza estado com localStorage e classe .dark no html</done>
</task>

<task type="auto">
  <name>Task 2: Classes dark: em home.blade.php e footer.blade.php</name>
  <files>resources/views/pages/home.blade.php, resources/views/partials/footer.blade.php</files>
  <read_first>
    - resources/views/pages/home.blade.php (ler arquivo completo — identificar TODOS os elementos com cores hardcoded)
    - resources/views/partials/footer.blade.php (ler arquivo completo)
    - .planning/phases/06-dark-light-mode/06-CONTEXT.md (decisões D-04, D-05, D-06)
  </read_first>
  <action>
Editar resources/views/pages/home.blade.php e resources/views/partials/footer.blade.php adicionando prefixos dark: em todos os elementos com cores hardcoded. O estado sem .dark = light mode (padrão). O estado com .dark = dark mode.

**Mapeamento de substituições — home.blade.php:**

Regra geral: cores dark hardcoded viram `dark:cor-original`. A cor sem prefixo = light mode.

| Cor atual | Versão light (sem prefixo) | Versão dark (com prefixo) |
|-----------|---------------------------|--------------------------|
| `bg-bg-primary` | manter (remapeado no CSS) | — |
| `bg-bg-card` | manter (remapeado no CSS) | — |
| `text-white` | `text-gray-900 dark:text-white` | — |
| `text-gray-300` | `text-gray-600 dark:text-gray-300` | — |
| `text-gray-400` | `text-gray-500 dark:text-gray-400` | — |
| `text-gray-500` | `text-gray-500 dark:text-gray-500` (sem mudança) | — |
| `bg-gray-800` | `bg-gray-200 dark:bg-gray-800` | — |
| `border-gray-800` | `border-gray-200 dark:border-gray-800` | — |
| `border-gray-700` | `border-gray-300 dark:border-gray-700` | — |
| `bg-gray-800/30` (form error) | `bg-gray-200/30 dark:bg-gray-800/30` (se existir) | — |

**Elementos específicos a modificar em home.blade.php:**

1. **Seção Hero** (linha ~6): A seção usa `bg-bg-primary` (custom property — já remapeada no CSS). Nenhuma mudança na seção em si. Porém os textos hardcoded:
   - `text-white` em `<h1>` → `text-gray-900 dark:text-white`
   - `text-gray-400` no slogan → `text-gray-500 dark:text-gray-400`

2. **Seção About** (linha ~59):
   - `text-white` em `<h2>` → `text-gray-900 dark:text-white`
   - `text-gray-300` nos parágrafos bio → `text-gray-600 dark:text-gray-300`
   - Botão CV: `border-accent text-accent` — manter (accent é o mesmo em ambos os temas)

3. **Seção Skills** (linha ~117):
   - `text-white` em `<h2>` → `text-gray-900 dark:text-white`
   - `text-gray-400` no subtítulo → `text-gray-500 dark:text-gray-400`
   - Botões prev/next: `bg-gray-800 hover:bg-accent border border-gray-700` → `bg-gray-200 dark:bg-gray-800 hover:bg-accent border border-gray-300 dark:border-gray-700 text-gray-700 dark:text-white`
   - Skill card: `border-gray-800` → `border-gray-200 dark:border-gray-800`
   - `text-gray-300` no nome da skill → `text-gray-600 dark:text-gray-300`

4. **Seção Projects** (linha ~200):
   - `text-white` em `<h2>` → `text-gray-900 dark:text-white`
   - `text-gray-400` no subtítulo e descrição dos cards → `text-gray-500 dark:text-gray-400`
   - `text-white` em `<h3>` card title → `text-gray-900 dark:text-white`
   - `border-gray-800` no card → `border-gray-200 dark:border-gray-800`
   - `bg-gray-800` no aspect-video placeholder → `bg-gray-200 dark:bg-gray-800`

5. **Seção Contact** (linha ~279):
   - `text-white` em `<h2>` e `<h3>` → `text-gray-900 dark:text-white`
   - `text-gray-300` em labels → `text-gray-600 dark:text-gray-300`
   - `text-gray-400` subtítulo → `text-gray-500 dark:text-gray-400`
   - Inputs: `border-gray-700` → `border-gray-300 dark:border-gray-700`; `text-white` → `text-gray-900 dark:text-white`; `placeholder-gray-500` manter
   - Ícones sociais containers: `border-gray-700` → `border-gray-300 dark:border-gray-700`
   - Caixa "Disponível": `border-gray-700` → `border-gray-300 dark:border-gray-700`
   - `text-white font-medium` no texto da disponibilidade → `text-gray-900 dark:text-white font-medium`

**footer.blade.php:**

6. **Botão back-to-top** (linha ~22):
   - Sem mudança no botão (bg-accent é consistente)

7. **Footer** (linha ~31):
   - `border-t border-gray-800` → `border-t border-gray-200 dark:border-gray-800`
   - `text-gray-500` no copyright → manter (mesmo em ambos os temas)

**IMPORTANTE:** As classes `bg-bg-primary` e `bg-bg-card` NÃO precisam de dark: prefix — elas são custom properties remapeadas pelo Plan 01 via CSS. Apenas as cores Tailwind hardcoded (`text-white`, `text-gray-300`, `bg-gray-800`, `border-gray-800`, etc.) precisam de dual classes.
  </action>
  <verify>
    <automated>grep -c "dark:text-white" /c/Users/Ygor/portifolio/resources/views/pages/home.blade.php && grep -c "dark:border-gray-800" /c/Users/Ygor/portifolio/resources/views/pages/home.blade.php && grep -n "dark:border-gray-800" /c/Users/Ygor/portifolio/resources/views/partials/footer.blade.php</automated>
  </verify>
  <acceptance_criteria>
    - home.blade.php contém pelo menos 5 ocorrências de `dark:text-white` (h1 hero, h2 sobre, h2 skills, h2 projetos, h2 contato, h3 contato, etc.)
    - home.blade.php contém pelo menos 3 ocorrências de `dark:border-gray-800` (cards de projeto, skill card, seção contato)
    - home.blade.php contém pelo menos 3 ocorrências de `dark:text-gray-300` (parágrafos bio, labels, nome skills)
    - home.blade.php contém pelo menos 2 ocorrências de `dark:bg-gray-800` (botões swiper, placeholder de imagem)
    - footer.blade.php contém `dark:border-gray-800` na tag `<footer>`
    - Nenhuma ocorrência de `text-white` sem `dark:` prefix em elementos de texto de conteúdo (só accent e botões primários podem ter text-white sem dark:)
    - home.blade.php NÃO remove as classes `bg-bg-primary` e `bg-bg-card` (essas devem permanecer)
  </acceptance_criteria>
  <done>home.blade.php e footer.blade.php com dual classes light/dark em todos os elementos com cores hardcoded; light mode exibe fundo claro e texto escuro corretamente</done>
</task>

</tasks>

<verification>
Verificação final após ambas as tasks:

1. Build passa: `cd /c/Users/Ygor/portifolio && npm run build` — sem erros
2. Contagem de dark: classes em home: `grep -c "dark:" resources/views/pages/home.blade.php` — deve retornar > 20
3. Toggle na nav: `grep "dark = !dark" resources/views/partials/nav.blade.php` — 2 ocorrências (desktop + mobile)
4. localStorage write: `grep "localStorage.setItem" resources/views/partials/nav.blade.php` — 2 ocorrências ('dark' e 'light')
</verification>

<success_criteria>
- Toggle sol/lua visível e funcional em desktop e mobile
- Clicar no toggle alterna tema com transição 150ms
- Tema escolhido persiste após reload (localStorage)
- Todos os textos, cards, navbar e footer refletem corretamente o tema claro
- Nenhum elemento fica invisível (texto branco em fundo branco) em light mode
</success_criteria>

<output>
Após conclusão, criar .planning/phases/06-dark-light-mode/06-02-SUMMARY.md
</output>
